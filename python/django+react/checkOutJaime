preparar API? django-admin startapp --api-- (darle nombre)

Cambios en BBDD?   ---  python .\manage.py makemigrations
arrancar servidor?  --- python .\manage.py runserver 


recuerda empaquetarlo antes de sacarlo  --  npm run dev, actualiza servidor y vuelve a solicitar GET
y limpiar la cache almenos en chrome


Cont vid 7

No me coge el ni el "ID" ni "host" ni "code", algo no esta bien enlazado.

Ahora si lo coge, pero no pasa la "logica" de si ya esta creado o no.
Puede ser que el problema este o bien en el "asignador de HOST & ID" o bien en el sserializer

Fijarse en NODE lo que dice.
            |
            |
            |
            V
OperationalError at /api/create-room

server output-> table api_room has no column named votes_to_skip
