<!-- 
Lo primero es llamar a expres a nuestro documento
despues se coloca dentro de una variable para poder llamarlo

Con el .get es como lo llamamos a realizar la funcion deseada

Para seleccionar todos los no etiquetados en los .get, poner "*".

Flujo de ejecución importa


Nodemon, hace que en vez de tener  que reiniciar el servidor, 
se "actualiza" sin tener que reiniciar el servidor entero,
con refrescar la página basta.

Para ejecutarlo igual que node; nodemon "app.js"

con los ":"
se le dice que todo lo que vayas detras de los :
está incluido en la seccion ya indicada


Para ir cogiendo info dáda al servidor, hay que envolverla en una 
variable, una vez envuelta ya podemos jugar con ella como otro objeto, ej;
var/let identificacion = req.parms."nombre_de_parametro"



-->